# Archive

Historical, superseded artifacts kept for reference only — not current documentation.

- `audit/` — the 2026-06 codebase audit (per-area reports A1-A6, MASTER-AUDIT triage,
  baseline) and the C3 token-storage migration plan. The findings have been enacted; see
  the current docs in `docs/` for the live state.
- `superpowers/` — design specs and plans from earlier milestones (packaging, profile-sync,
  scheduling, Airflow demo, error-handling).

Current authoritative docs live in `docs/` (PROJECT-SUMMARY, data-residency,
connecting-your-database) and the repo root (README, DEPLOY, LAUNCH, PRODUCT).
