n
demo:
  type: postgres
  host: ${DEMO_DB_HOST}
  port: 5432
  database: ${DEMO_DB_NAME}
  username: ${DEMO_DB_USER}
  password: ${DEMO_DB_PASSWORD}
  schema: public
  connect_args:
    sslmode: require
    connect_timeout: 10
    application_name: aegis-demo
